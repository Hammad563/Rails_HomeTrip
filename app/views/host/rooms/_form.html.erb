<div data-controller="room">
    <template data-room-target="bedTemplate"> 
         <label  class="block text-sm font-medium text-gray-700">Bed Sizes</label>
         <select name="room[beds_attributes][][bed_size]" class="mt-1 block w-full pl-3 pr-10 py-2 text-base border-gray-300 focus:outline-none focus:ring-indigo-500 focus:border-indigo-500 sm:text-sm rounded-md">
            <%  Bed.bed_sizes.each do |bed_size, _| %>
                <option><%= bed_size %></option>
            <% end %>
        </select>
    </template>


    <form action="<%= host_listing_rooms_path(listing) %>" method="post">
    <input type="hidden" name="authenticity_token" value="<%= form_authenticity_token %>">

    <label for="room_type" class="block text-sm font-medium text-gray-700">Room type</label>
    <select id="room_type" name="room[room_type]" class="mt-1 block w-full pl-3 pr-10 py-2 text-base border-gray-300 focus:outline-none focus:ring-indigo-500 focus:border-indigo-500 sm:text-sm rounded-md">
      <% Room.room_types.each do |rt, _| %>
      <option><%= rt %></option>
      <% end %>
    </select>

    <fieldset data-room-target="beds">
      <legend>Beds</legend>
    </fieldset>

    <a class="bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded-full" href="#" data-action="room#addBed">add bed</a>

    <button class="bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded-full" type="submit" value="Add room">Add Room</button>
</form>
</div>